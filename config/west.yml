manifest:
  remotes:
    - name: phy-sandbox
      url-base: https://github.com/phy-sandbox
  projects:
    - name: zmk
      remote: phy-sandbox
      revision: main
      import: app/west.yml
  self:
    path: config
